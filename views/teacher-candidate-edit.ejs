<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> — Edit Candidate</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Inter', sans-serif; background: #111111; color: #ffffff; min-height: 100vh; display: flex; flex-direction: column; align-items: center; }
    a { text-decoration: none; color: inherit; }

    .container { width: 100%; max-width: 600px; padding: 24px; }

    .header { display: flex; align-items: center; justify-content: space-between; padding: 16px 0; margin-bottom: 24px; }
    .header h1 { font-size: 1.25rem; font-weight: 600; }
    .header a { font-size: 0.875rem; color: #a3a3a3; }
    .header a:hover { color: #ffffff; }

    .card { background: #09090b; border: 1px solid #262626; border-radius: 10px; padding: 28px; margin-bottom: 20px; }
    .card-title { font-size: 0.7rem; font-weight: 600; color: #71717a; text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 16px; }

    .form-group { margin-bottom: 16px; }
    .form-group label { display: block; font-size: 0.8rem; font-weight: 500; color: #a3a3a3; margin-bottom: 6px; }
    .form-group input, .form-group textarea {
      width: 100%; padding: 10px 12px;
      background: #111111; border: 1px solid #262626; border-radius: 6px;
      font-size: 0.85rem; font-family: 'Inter', sans-serif;
      color: #ffffff; outline: none;
    }
    .form-group input:focus, .form-group textarea:focus { border-color: #8A5DF4; }
    .form-group textarea { min-height: 80px; resize: vertical; }

    .form-row { display: flex; gap: 12px; }
    .form-row .form-group { flex: 1; }

    .btn {
      height: 40px; padding: 0 24px; border: none; border-radius: 6px;
      font-size: 0.85rem; font-weight: 500; font-family: 'Inter', sans-serif;
      cursor: pointer; transition: background 0.15s;
    }
    .btn-primary { background: #8A5DF4; color: #ffffff; }
    .btn-primary:hover { background: #7c3aed; }
    .btn-small { height: 32px; padding: 0 14px; font-size: 0.75rem; }
    .btn-danger { background: #ef4444; color: #ffffff; }
    .btn-danger:hover { background: #dc2626; }

    .success-msg {
      font-size: 0.85rem; color: #22c55e; text-align: center; margin-bottom: 16px;
      padding: 12px; background: rgba(34,197,94,0.08);
      border: 1px solid rgba(34,197,94,0.2); border-radius: 6px;
    }
    .error-msg {
      font-size: 0.85rem; color: #ef4444; text-align: center; margin-bottom: 16px;
      padding: 12px; background: rgba(239,68,68,0.08);
      border: 1px solid rgba(239,68,68,0.2); border-radius: 6px;
    }

    .member-item {
      display: flex; align-items: center; gap: 10px;
      padding: 8px 0; border-bottom: 1px solid #262626;
    }
    .member-item:last-child { border-bottom: none; }
    .member-item .info { flex: 1; }
    .member-item .name { font-size: 0.85rem; font-weight: 500; }
    .member-item .role { font-size: 0.75rem; color: #71717a; margin-top: 1px; }

    .divider { height: 1px; background: #262626; margin: 20px 0; }

    @media (max-width: 480px) {
      .container { padding: 16px; }
      .card { padding: 20px; }
      .form-row { flex-direction: column; gap: 0; }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Edit Candidate</h1>
      <a href="/teacher">Back to Dashboard</a>
    </div>

    <% if (success) { %><div class="success-msg"><%= success %></div><% } %>
    <% if (error) { %><div class="error-msg"><%= error %></div><% } %>

    <div class="card">
      <div class="card-title">Candidate Info</div>
      <form method="POST" action="/teacher/candidate/edit/<%= candidate.CID %>">
        <div class="form-row">
          <div class="form-group">
            <label for="cname">Full Name</label>
            <input type="text" id="cname" name="cname" value="<%= candidate.CNAME %>" required>
          </div>
          <div class="form-group">
            <label for="position">Position</label>
            <input type="text" id="position" name="position" value="<%= candidate.POSITION || '' %>" placeholder="e.g. President">
          </div>
        </div>
        <div class="form-group">
          <label for="sid">Student ID</label>
          <input type="text" id="sid" name="sid" value="<%= candidate.SID || '' %>" placeholder="e.g. SBA-2501">
        </div>
        <div class="form-group">
          <label for="desc">Description / Platform</label>
          <textarea id="desc" name="desc"><%= candidate.DESC || '' %></textarea>
        </div>
        <button type="submit" class="btn btn-primary">Save Changes</button>
      </form>
    </div>

    <div class="card">
      <div class="card-title">Campaign Members</div>

      <% if (members && members.length > 0) { %>
        <% members.forEach(function(m) { %>
          <div class="member-item">
            <div class="info">
              <div class="name"><%= m.MNAME %></div>
              <div class="role"><%= m.POSITION || '—' %></div>
            </div>
            <form method="POST" action="/teacher/candidate/member/delete" onsubmit="return confirm('Remove <%= m.MNAME %>?')">
              <input type="hidden" name="member_id" value="<%= m.ID %>">
              <input type="hidden" name="cid" value="<%= candidate.CID %>">
              <button type="submit" class="btn btn-danger btn-small">Remove</button>
            </form>
          </div>
        <% }); %>
        <div class="divider"></div>
      <% } else { %>
        <div style="font-size:0.85rem;color:#71717a;margin-bottom:16px;">No campaign members yet.</div>
        <div class="divider"></div>
      <% } %>

      <div class="card-title" style="margin-top:0;">Add Campaign Member</div>
      <form method="POST" action="/teacher/candidate/member/add">
        <input type="hidden" name="cid" value="<%= candidate.CID %>">
        <div class="form-row">
          <div class="form-group">
            <label for="mname">Full Name</label>
            <input type="text" id="mname" name="mname" placeholder="e.g. John Doe" required>
          </div>
          <div class="form-group">
            <label for="role">Role</label>
            <input type="text" id="role" name="role" placeholder="e.g. Campaign Manager">
          </div>
        </div>
        <button type="submit" class="btn btn-primary">Add Member</button>
      </form>
    </div>
  </div>
</body>
</html>
